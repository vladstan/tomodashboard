schema {
  query: Query
}

interface Node {
  id: ID!
}

type Profile implements Node {
  id: ID!
  _id: String
  userId: String
}

type Query {
  node(id: ID!): Node
  user(id: String): User
}

type User implements Node {
  id: ID!
  _id: String
  facebookId: String
  profile: Profile
}
